# AI Employee — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to git. It is listed in .gitignore.

# ---------------------------------------------------------------------------
# Gmail Watcher
# ---------------------------------------------------------------------------
# Path to the OAuth 2.0 credentials JSON downloaded from Google Cloud Console.
# See: https://console.cloud.google.com → APIs & Services → Credentials
GMAIL_CREDENTIALS=/absolute/path/to/credentials.json

# ---------------------------------------------------------------------------
# LinkedIn Watcher
# ---------------------------------------------------------------------------
# Directory where the persistent Chromium session for LinkedIn is stored.
LINKEDIN_SESSION_PATH=/home/your_user/.linkedin_session

# Set to "false" for first-run login (opens a browser window).
# Set to "true" (default) for normal headless operation.
LINKEDIN_HEADLESS=true
